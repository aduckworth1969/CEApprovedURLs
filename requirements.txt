# Dependencies for build_approved_sites.py

# CSV reading
pandas

# Fetching site descriptions and favicons
requests
beautifulsoup4

# Optional: progress bar during the build (the script runs without it)
tqdm
